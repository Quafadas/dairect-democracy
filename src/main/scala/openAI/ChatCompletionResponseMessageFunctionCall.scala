package openAI

import smithy4s.Hints
import smithy4s.Schema
import smithy4s.ShapeId
import smithy4s.ShapeTag
import smithy4s.schema.Schema.string
import smithy4s.schema.Schema.struct

/** The name and arguments of a function that should be called, as generated by the model.
  * @param name
  *   The name of the function to call.
  * @param arguments
  *   The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function.
  */
final case class ChatCompletionResponseMessageFunctionCall(name: Option[String] = None, arguments: Option[String] = None)
object ChatCompletionResponseMessageFunctionCall extends ShapeTag.Companion[ChatCompletionResponseMessageFunctionCall] {
  val id: ShapeId = ShapeId("openAI", "ChatCompletionResponseMessageFunctionCall")

  val hints: Hints = Hints(
    smithy.api.Documentation("The name and arguments of a function that should be called, as generated by the model."),
  )

  implicit val schema: Schema[ChatCompletionResponseMessageFunctionCall] = struct(
    string.optional[ChatCompletionResponseMessageFunctionCall]("name", _.name).addHints(smithy.api.Documentation("The name of the function to call.")),
    string.optional[ChatCompletionResponseMessageFunctionCall]("arguments", _.arguments).addHints(smithy.api.Documentation("The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function.")),
  ){
    ChatCompletionResponseMessageFunctionCall.apply
  }.withId(id).addHints(hints)
}
